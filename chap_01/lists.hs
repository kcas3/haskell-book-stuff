lostNumbers = [4,8,15,16,23,42] -- assigned in the gchi with let then the name
concatFunc x y = x ++ y -- ++ concatinates. Works with strings and lists bc
                        -- strings are really just lists of characters